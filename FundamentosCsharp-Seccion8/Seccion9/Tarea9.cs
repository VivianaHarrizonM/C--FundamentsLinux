using System.Text;

namespace Seccion9
{
  internal class Tarea9
  {
  /*  static void Main(string[] args)
    {
      //Introducir una cadena de caracteres e indicar si es un polindromo. (una palabra polindroma es aquella que se lee igual adelante que atrás) por ejemplo Ana,ojo, rayar, solos, rapar, oso, rotomotor, sedes.

      string palabraUsuario, palabraInvertida = "";

      //Pedimos la palabra
      Console.Write("Ingresa una palabra y veamos si es un polindromo: ");
      palabraUsuario = Console.ReadLine();

      foreach (char letra in palabraUsuario)
      {
        palabraInvertida = letra + palabraInvertida;
      }

      if (palabraUsuario.Equals(palabraInvertida, StringComparison.OrdinalIgnoreCase))
      {
        Console.WriteLine($"\"{palabraUsuario}\"es una palabrapalindroma");
      }
      else
      {
        Console.WriteLine($"\"{palabraUsuario}\"No es una palabrapalindroma");
      }
    }*/
    
  }
} 